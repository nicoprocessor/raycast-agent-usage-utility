{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "raycast-agent-usage-utility",
  "title": "Raycast Agent Usage Utility",
  "description": "Track remaining AI provider quota percentages across Anthropic, OpenAI Codex, and GitHub Copilot.",
  "icon": "icon.png",
  "author": "nicoprocessor",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "platforms": [
    "macOS"
  ],
  "commands": [
    {
      "name": "add-provider",
      "title": "Add Provider",
      "subtitle": "Agent Usage",
      "description": "Add a provider and securely save its token in Keychain",
      "mode": "view"
    },
    {
      "name": "usage-status",
      "title": "Usage Status",
      "subtitle": "Agent Usage",
      "description": "Show remaining provider quota percentages and reset windows",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.10",
    "@raycast/utils": "^2.2.3"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.1.1",
    "@types/node": "^25.5.0",
    "eslint": "^10.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured\"",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml,css,scss}": [
      "prettier --write"
    ]
  }
}
